{% extends ':Common:span8nonav.html.twig' %}

{% block mainSpan %}
<h1>New location</h1>

<form action="{{ path('location_create') }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <div>
        <button type="submit" class="btn btn-primary">Create</button>
        <a href="{{ path('location') }}" class="btn">Cancel</a>
    </div>
</form>
{% endblock %}
